# @technical-1/core

## 0.1.0

### Minor Changes

- 1bbfebd: Initial release: typed error hierarchy (`PptrKitError` + subclasses with a
  `retryable` flag), `Logger` interface, and shared option shapes.
