import { describe, it, expect } from "vitest";

// Placeholder so `vitest run` finds a test file before the real surface exists.
// Removed in Task 6 once index.test.ts (the surface lock) is added.
describe("cdp scaffold", () => {
  it("package is wired", () => {
    expect(true).toBe(true);
  });
});
