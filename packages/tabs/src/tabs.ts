/**
 * Scaffold placeholder for the tabs capability package.
 *
 * Real implementation (waitForNewPage / waitForPageMatching) lands in
 * Task 2-3; this stub exists only so the build tooling (tsc, vitest,
 * tsup) has an input file and the package's test/coverage gates stay
 * green during scaffolding.
 */
export const TABS_SCAFFOLD_VERSION = "0.0.0";
